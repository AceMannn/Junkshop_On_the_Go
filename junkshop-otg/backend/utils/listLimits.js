/** Default cap for dashboard list endpoints (pickups, transactions). */
const PICKUP_LIST_LIMIT = 100;
const TRANSACTION_LIST_LIMIT = 100;

module.exports = {
  PICKUP_LIST_LIMIT,
  TRANSACTION_LIST_LIMIT,
};
