# API base URL (must match backend PORT)
VITE_API_BASE_URL=http://localhost:5000

# Shown when an admin tries to log in on the public site
VITE_ADMIN_PORTAL_URL=http://localhost:5175

# Optional: Google Maps API (not required — app uses free OpenStreetMap + Leaflet)
# VITE_GOOGLE_MAPS_API_KEY=
